# Work

这里保存为完成具体目标而产生的工作内容，不保存可跨任务复用的长期知识。

- `tasks/`：当前任务、行动项、上下文包和任务内决策
- `outputs/`：报告、方案、清单和其他交付物
- `archive/`：已结束且不再活跃的任务与历史结果

任务状态写在 frontmatter 中，不靠移动目录表示。任务结束后，只把可复用增量整理到 `knowledge/`，不默认保存完整对话。
